# Global Analytics Dashboard — Web
# Copy to .env and fill in values. Never commit .env.
#
#   cp .env.example .env

# ── Internal API ─────────────────────────────────────────────
# Base URL of the NestJS backend (no trailing slash)
NEXT_PUBLIC_API_URL=http://localhost:3001
